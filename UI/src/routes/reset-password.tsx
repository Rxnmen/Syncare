import { createFileRoute, Link, useNavigate } from "@tanstack/react-router";
import { useEffect, useState } from "react";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { supabase } from "@/integrations/supabase/client";
export const Route = createFileRoute("/reset-password")({ head: () => ({ meta: [{ title: "Reset password — VitaStudent" }, { name: "description", content: "Set a new secure VitaStudent password." }, { property: "og:title", content: "Reset password — VitaStudent" }, { property: "og:description", content: "Set a new secure VitaStudent password." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: ResetPassword });
function ResetPassword() { const [valid, setValid] = useState(false); const [error, setError] = useState(""); const navigate = useNavigate(); useEffect(() => { setValid(window.location.hash.includes("type=recovery")); }, []); const submit = async (e: React.FormEvent<HTMLFormElement>) => { e.preventDefault(); const password = String(new FormData(e.currentTarget).get("password") ?? ""); if (password.length < 8) { setError("Use at least 8 characters."); return; } const { error: authError } = await supabase.auth.updateUser({ password }); if (authError) setError(authError.message); else navigate({ to: "/auth" }); }; return <main className="grid min-h-screen place-items-center bg-background px-4"><div className="w-full max-w-md rounded-xl border border-border bg-card p-6 shadow-card"><h1 className="font-display text-2xl font-semibold">Choose a new password</h1><p className="mt-2 text-sm text-muted-foreground">Use at least eight characters you don’t use elsewhere.</p>{valid ? <form onSubmit={submit} className="mt-6 space-y-4"><div className="space-y-2"><Label htmlFor="new-password">New password</Label><Input id="new-password" name="password" type="password" minLength={8} required /></div>{error && <p className="text-sm text-destructive">{error}</p>}<Button className="w-full">Update password</Button></form> : <div className="mt-6"><p className="rounded-lg bg-warning-soft p-3 text-sm">This reset link is missing or has expired.</p><Button asChild variant="outline" className="mt-4 w-full"><Link to="/auth">Request a new link</Link></Button></div>}</div></main>; }